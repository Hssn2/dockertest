@{
    ViewData["Title"] = "Ana Sayfa";
}

<div class="text-center">
    <h1 class="display-5">Merhaba, Özgür Akdeniz</h1>
    <p class="lead text-muted">ASP.NET Core MVC + Docker release pipeline deneme uygulaması</p>
    <span class="badge bg-primary">v@(ViewBag.AppVersion)</span>
    <p class="mt-4">
        <code>release</code> branch'ine commit atınca GitHub Actions otomatik Docker image üretir.
    </p>
</div>
